#include <stdio.h>
#include <stdlib.h>
#include "test.h"
int main()
{
    int x=10;
    int y=20;
    printf("sum of %d & %d = %d \n", x,y , SumValues(x,y) );
    printf("sum of %d & %d = %d \n", x,y , multiply_values(x,y) );
    return 0;
    // 3aiz a3ml comitt
}
